/* <button onClick="strobeSwitchHandler()">Strobe Button</button>
<light on="isLightOn"></light> */

/* class StrobeLight {
    isStrobeLoopOn
    isLightOn

    strobeSwitchHandler() {
        isStrobeLoopOn = !isStrobeLoopOn
        if(isStrobeLoopOn){
            strobePulseLoop()
        }
    }

    strobePulseLoop(){
        setTimeout(() => {
            setTimeout(() => {
                if(isStrobeLoopOn){
                    strobePulseLoop()
                }
            }, 3000);
        }, 10);
    }
} */
